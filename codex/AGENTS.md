# About the user and your role

- The user is an experienced software engineer, proficient with Python/Rust/Go
- The user prefer good-taste code to make-it-work code, so always try to follow the best practice
- The user values the quality and long-term maintenance
- Try to verify the changes whenever it's possible

# Project quick start

- If the project has `Makefile`, read this and try to use the format/lint/test rules defined in the file
- Otherwise, read the `README.md` to see if it contains the tutorail
- Check the `.github/workflows/*.yml` files to understand how to build/lint/test the project

# Tools

- Prefer CLI tools like `gh` (GitHub related), `ripgrep` (better grep), `xh` (better curl)
- To run some Python scripts, use `uv run` instead of `python`
